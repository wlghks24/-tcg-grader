name: Legacy Main ↔ Instagram Crosscheck (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-by-policy:
    runs-on: ubuntu-latest
    steps:
      - name: Cross-domain audit retired
        run: |
          echo "Disabled by user policy: Instagram card-info no longer cross-validates against card-price analysis."
          echo "Use instagram_tcg_content/source_verification_engine.py and source_route_resilience.py for Instagram-local evidence verification."
