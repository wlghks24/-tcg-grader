name: Legacy Main Crosscheck Snapshot Persistence (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-by-policy:
    runs-on: ubuntu-latest
    steps:
      - name: Main snapshot persistence retired
        run: |
          echo "Disabled by user policy: Instagram card-info no longer consumes or cross-validates MARKET_ANALYSIS snapshots."
          echo "No artifact download, contents write, or factual snapshot persistence is performed."
